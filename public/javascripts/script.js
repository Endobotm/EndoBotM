window.addEventListener("load", function () {
  /*creates a event listener that detects the load event*/ const loader =
    document.querySelector(
      ".loader"
    ); /*selects the element with class="loader"*/
  loader.className += " hidden"; /*sets class name to "loader hidden"*/
});
